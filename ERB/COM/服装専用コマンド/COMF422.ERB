;-------------------------------------------------
;穢れを受ける
;服装固有コマンド
;シスター属性の服限定
;　　　＆淫妖経験と信仰経験が50以上
;-------------------------------------------------
@COM422

;消費CP
LOSEBASE:0 = 4

IF BASE:ＣＰ < LOSEBASE:0
	PRINTL CPが足りない
	RETURN 0
ENDIF

PRINTL 穢れを受ける
STR:0 = 穢れを受ける

CALL KOJO_MESSAGE_COM
;挿入処理を行う
;[シスター]の場合は依存を得る
IF TALENT:シスター
	CALL INSERT, 2, 0
;そこまで行ってない場合は傷物に
ELSE
	CALL INSERT, 2, 1
ENDIF

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

SOURCE:信仰 = 10
SOURCE:淫妖 = 10
SOURCE:背徳 = 8

TFLAG:信仰経験 = 9
TFLAG:淫妖経験 = 9
TFLAG:背徳経験 = 8
TFLAG:精液経験 = 2
TFLAG:精飲要素 = 2
LOSEBASE:アライメント = -1
;穢れ加算処理
CALL CALC_IMP, 1


TFLAG:性交 = 2

RETURN 1

