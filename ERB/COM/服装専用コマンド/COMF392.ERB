;-------------------------------------------------
;民と話す
;姫様専用コマンド
;$400～700入手できる。疎通経験と疎通も得られる。ただし誘拐蓄積値+3
;-------------------------------------------------
@COM392
#DIM GET_MONEY, 1

;消費CP
LOSEBASE:0 = 8

IF BASE:ＣＰ < LOSEBASE:0
	PRINTL CPが足りない
	RETURN 0
ENDIF

PRINTL 民と話す
STR:0 = 民と話す
CALL KOJO_MESSAGE_COM

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;気品+プライド-200の数値×3％が成功率
CALL SUC_PAR, (BASE:気品+BASE:プライド-200)*3

PRINTL 
IF RESULT == 1
	SOURCE:美麗 = 9
	SOURCE:疎通 = 8
	
	TFLAG:美麗経験 = 7
	TFLAG:疎通経験 = 6
	
	GET_MONEY = 400 + RAND:5
ELSE
	SOURCE:疎通 = 4
	
	TFLAG:疎通経験 = 3
	GET_MONEY = 100 + RAND:2
ENDIF

;強力なコマンドだが、使えば使うほど誘拐されやすくなる
;(売春等のコマンドはデメリット持ちなので1回の教育で3～9％上がるだけで済む)
CFLAG:誘拐蓄積値 += 3

RETURN 1

